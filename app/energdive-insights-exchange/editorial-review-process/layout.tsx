import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Editorial & Review Process | ENERGDIVE Insights Exchange",
    description: "Understand how the ENERGDIVE Insights Exchange reviews and evaluates submissions through a staged editorial process focused on relevance, quality, and industry value.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
