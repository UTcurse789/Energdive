import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Author Guidelines | ENERGDIVE Insights Exchange",
    description: "Submission requirements, abstract guidelines, recommended paper lengths, format specifications, and editorial evaluation criteria for the ENERGDIVE Insights Exchange.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
    return children;
}
